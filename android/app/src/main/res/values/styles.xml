<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Splash "clasico" (Android <=11): imagen completa de marca -->
        <item name="android:background">@drawable/splash</item>
        <!-- Splash Screen API de Android 12+: fondo solido + icono de la app
             (Android fuerza este formato, no permite un poster completo) -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <!-- Drawable dedicado y sin variantes por orientacion (a diferencia
             de @drawable/splash, que tiene versiones drawable-land-*/-port-*
             recortadas distinto) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>